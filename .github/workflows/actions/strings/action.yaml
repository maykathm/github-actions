name: 'Cool Strings'
description: 'Do cool things with strings'
inputs:
  something-extra:
    default: "cool"
runs:
  using: "composite"
  steps:
    - name: Hello
      run: echo "Hello, world"
      shell: bash
    
    - name: dude
      run: echo "Hello, person. Here's something extra ${{ inputs.something-extra }}"
      shell: bash